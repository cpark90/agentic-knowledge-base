# CLAUDE.md — 이 저장소에서 일하는 방법

하네스 운영 규칙의 원본은 **`AGENTS.md`**, 컴포넌트별 작성 스타일의 원본은
**`STYLEGUIDE.md`**다. 세션 시작 시 두 문서를 읽고 그대로 따른다 — 이 파일은
Claude Code용 진입점이며 두 문서를 재진술하지 않는다.

요약(원본은 위 두 문서):

1. 지식 파일을 고치면 `bazel test //...` — 실패하면 게이트가 아니라 산출물을 고친다.
2. 어휘 밖 술어 금지 — 새 개념은 온톨로지 청크(파일) 추가로 먼저.
3. 스코프·가정은 ODD(`odd/project-odd.ttl`)의 조건만 참조 — 필요하면 ODD 먼저 확장.
4. 한 청크는 한 파일(frontmatter + 본문 ≤ 42줄). 청크 head 그래프는 생성물이다 —
   `kg/`에 손으로 쓰지 않는다.
5. 역할·권한(`AGENTS.md` 표)의 형식 원본은 `kg/catalog-kg.ttl` — 문서와 그래프를
   같은 커밋에서 일치시킨다.
6. 참조 저장소(`../harness_ontology`, `../harness-recipes`)는 내용의 원천일 뿐 빌드
   의존이 아니다 — 지식을 `agt:` 청크로 승격하고 `derived_from`으로 출처를 남긴다.
7. 산문은 한글, 식별자·라벨(en)은 영어. 지어낸 용어 금지 (노트 0.0절, 0.6절).

체계의 설계 근거가 필요하면 `agent-knowledge-system-notes.md`의 해당 절을 연다 —
통째로 컨텍스트에 싣지 않는다.
