load("//defs:knowledge.bzl", "kb_gate_test")

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "odd",
    srcs = ["project-odd.ttl"],
)

# ODD 게이트 (3.2절): 모든 조건에 판정 방법·등급, ODD에 mode와 조건 최소 1
kb_gate_test(
    name = "gate_test",
    odd = [":odd"],
    ontology = ["//ontology:modules"],
    shapes = ["//ontology:shapes"],
)
