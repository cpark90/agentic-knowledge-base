# 생성 파일 — 손으로 고치지 않는다. 원본은 각 청크의 frontmatter (tools/gen_build.py). 검사: //:build_drift_test
load("//defs:kb.bzl", "kb_ontology_module")

package(default_visibility = ["//visibility:public"])

# 모듈 = 디렉토리, 청크 = 파일 (2.3절). IRI https://agentic-knowledge-base.dev/ontology/related/assumption
kb_ontology_module(
    name = "assumption",
    srcs = glob(["*.ttl"]),
    iri = "https://agentic-knowledge-base.dev/ontology/related/assumption",
)
