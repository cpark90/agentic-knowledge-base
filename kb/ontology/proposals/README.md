# proposals — 용어 제안 승인 큐 (그래프 밖)

`bazel run //tools:term_propose`가 검사(상위 실재·라벨 중복·정의 형식)를 통과한 제안을 여기에 쓴다. 이 디렉토리는
`//kb/ontology:modules` 밖이라 승인 전에는 그래프에 들어가지 않는다 — 유저 승인 뒤 주제가 맞는 모듈 디렉토리의 새 파일로
옮긴다 (STYLEGUIDE §1, `docs/tools.md` 용어 제안 워크플로). 지금은 비어 있다.
